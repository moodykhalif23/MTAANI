
; Local overrides for CouchDB in Docker
; Keep admin and secret managed via environment variables:
;   COUCHDB_USER, COUCHDB_PASSWORD, COUCHDB_SECRET
; Add additional settings below if required.

[admins]
; Managed via Docker env (COUCHDB_USER/COUCHDB_PASSWORD). Remove or keep empty to avoid conflicts.
admin = -pbkdf2-80e7f823f09967a520df434c8754040cbe4bd067,500d3288a0fce6ea825bfb382eb699cf,10

[chttpd_auth]
secret = your_secret_key_here_32_chars_min

[chttpd]
bind_address = 0.0.0.0
port = 5984

[cluster]
n = 1

[admins]
admin = secure_admin_password_2024
